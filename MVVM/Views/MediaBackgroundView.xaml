<local:BaseView x:TypeArguments="local:MediaBackgroundModelView" x:Class="MediaPlayer.MediaBackgroundView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MediaPlayer"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MediaBackgroundView">
    <Grid Background="{StaticResource DarkBackgroundBrush}">
        <TextBlock Text="Hello"
                   Margin="10"
                   HorizontalAlignment="Center"
                   Foreground="White"/>
        <Image Source="Assets/Play.png" 
               VerticalAlignment="Center" 
               HorizontalAlignment="Center"
               Width="50" Height="50"/>
    </Grid>
</local:BaseView>
